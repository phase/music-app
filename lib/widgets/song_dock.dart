import 'package:flutter/material.dart';
import '../main.dart';
import '../model.dart';
import '../client.dart';

class SongDock extends StatefulWidget {
  final int songId;

  SongDock(this.songId);

  @override
  _SongDockState createState() => new _SongDockState();
}

class _SongDockState extends State<SongDock> {
  @override
  Widget build(BuildContext context) {
    return new Row(
      children: <Widget>[
//      widget.
      ],
    );
  }
}
